import {HashLink} from "react-router-hash-link";

const ProjectPage = () => {
  return(
      <div>
        <HashLink
            to="/#projects"
        >projects
        </HashLink>
      </div>
  )
};
export default ProjectPage;
